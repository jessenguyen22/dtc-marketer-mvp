# Brand Brain — DTC Master Context

> A single source of truth that every skill in this pack reads from. Fill it once, then every skill stays on-brand without you re-typing context.
>
> **How to use:** Copy this file, rename to `BRAND_BRAIN.md`, place in your project root (or `~/.claude/brand-brain/<brand>.md` for global). Replace every `<...>` placeholder. Keep it ≤ 4 pages — Claude reads it on every skill invocation.

---

## 1. Brand Identity

| Field | Value |
|---|---|
| Brand name | `<exact legal + display name>` |
| Founded | `<year>` |
| Founders | `<names + 1-line story>` |
| Mission (why we exist) | `<one sentence — the change we're trying to make>` |
| Vision (where we're going) | `<one sentence — the future we believe in>` |
| Core promise to customer | `<one sentence — what they always get>` |
| One-liner | `<≤12 words — for ads + bio>` |
| Elevator pitch (30s) | `<3-4 sentences — for press + investors>` |

**Origin story (≤100 words):** `<the founding moment a customer would re-tell>`

---

## 2. Positioning

- **Category we play in:** `<e.g. clean skincare, performance hydration>`
- **Frame of reference (what we're compared to):** `<incumbent or category>`
- **Point of difference:** `<the one thing only we do, in plain words>`
- **Reason to believe (RTB):** `<proof — patent, ingredient, founder cred, results>`
- **Positioning statement:** _For `<ICP>` who `<problem>`, `<brand>` is the `<category>` that `<benefit>` because `<RTB>`._
- **Top 3 competitors + how we beat each:**
  1. `<competitor>` — we win on `<axis>`
  2. `<competitor>` — we win on `<axis>`
  3. `<competitor>` — we win on `<axis>`
- **Positioning gap we exploit:** `<unmet customer need our category ignores>`

---

## 3. Voice & Tone

**Tone axes** (mark X on the spectrum):

```
serious        |---X-----------|  playful
formal         |--------X------|  casual
respectful     |---X-----------|  irreverent
matter-of-fact |-----X---------|  enthusiastic
expert         |---X-----------|  peer
```

- **Voice in 3 adjectives:** `<e.g. confident, warm, no-BS>`
- **We sound like:** `<2 reference brands or people>`
- **We do NOT sound like:** `<2 anti-references>`

**Signature phrases (always feel free to use):**
- `<phrase 1>`
- `<phrase 2>`
- `<phrase 3>`

**Banned words / phrases (never use):**
- `<word>` — reason
- `<word>` — reason
- `<jargon, hype, "game-changing", "revolutionary", etc.>`

**Capitalization & style:**
- Sentence case for headlines? `<yes / no>`
- Oxford comma? `<yes / no>`
- Emoji policy: `<allowed / banned / sparingly in social only>`
- Em-dash, exclamation, all-caps rules: `<...>`

---

## 4. Audience

### Primary ICP
- **Demo:** `<age, gender, income, geo, marital, kids>`
- **Psycho:** `<values, identity markers, daily frustrations, aspirations>`
- **Where they hang out:** `<subreddits, IG accounts, podcasts, retailers>`
- **What they're saying about the problem (verbatim quotes):**
  - "<quote 1>"
  - "<quote 2>"
  - "<quote 3>"

### Jobs To Be Done (JTBD)
1. **Functional:** `<the practical task>`
2. **Emotional:** `<how they want to feel>`
3. **Social:** `<how they want to be seen>`

### Top 5 Objections (and our pre-empted answer)
| # | Objection | Our 1-line answer |
|---|---|---|
| 1 | "<objection>" | "<answer>" |
| 2 | "<objection>" | "<answer>" |
| 3 | "<objection>" | "<answer>" |
| 4 | "<objection>" | "<answer>" |
| 5 | "<objection>" | "<answer>" |

### Switching triggers (what makes them leave the competitor)
- `<trigger 1>`
- `<trigger 2>`
- `<trigger 3>`

### Secondary ICP (optional)
`<one paragraph — only if a meaningful 2nd persona exists>`

---

## 5. Product

### Catalog snapshot
| SKU | Name | Hero? | Price | Margin % | Subscription? |
|---|---|---|---|---|---|
| `<sku>` | `<name>` | ✅ | $`<>` | `<%>` | yes/no |
| ... | | | | | |

- **Hero product:** `<name>` — `<why it's the wedge>`
- **Bundles:** `<bundle name>` — components + discount logic
- **Avg order value (AOV):** $`<>`
- **Subscription % of orders:** `<%>`

### Feature → Benefit → Outcome (top 5)
| Feature (what it has) | Benefit (what it does) | Outcome (what it means for them) |
|---|---|---|
| `<>` | `<>` | `<>` |
| `<>` | `<>` | `<>` |
| `<>` | `<>` | `<>` |
| `<>` | `<>` | `<>` |
| `<>` | `<>` | `<>` |

### Ingredients / materials / tech
- **Hero ingredient or component:** `<>` — what it does, why it matters
- **Sourcing story:** `<>`
- **Certifications:** `<>` (e.g. USDA Organic, Leaping Bunny, B-Corp, ISO)
- **What we DON'T put in / how we're different:** `<>`

### Comparison table vs main competitor
| Attribute | Us | `<Competitor>` |
|---|---|---|
| `<axis 1>` | ✅ | ❌ |
| `<axis 2>` | `<>` | `<>` |
| `<axis 3>` | `<>` | `<>` |
| Price | $`<>` | $`<>` |

---

## 6. Customer Journey

| Stage | Top sources | Top blocker | Top unlock |
|---|---|---|---|
| Awareness | `<paid social, podcast, organic>` | `<>` | `<>` |
| Consideration | `<reviews, comparison, founder story>` | `<>` | `<>` |
| Conversion | `<PDP, checkout, free-ship>` | `<>` | `<>` |
| First-order activation | `<unboxing, first-use, day-7 follow-up>` | `<>` | `<>` |
| Repeat / Subscription | `<replenish reminder, bundle upsell>` | `<>` | `<>` |
| Advocacy | `<referral program, UGC ask>` | `<>` | `<>` |

---

## 7. Channel Strategy

| Channel | Role | Owned/Paid | % budget | KPI |
|---|---|---|---|---|
| Meta Ads | acquisition + retargeting | paid | `<%>` | CAC, ROAS |
| Google Ads | branded + non-brand | paid | `<%>` | CPA, ROAS |
| TikTok Ads | top-funnel + UGC | paid | `<%>` | CPM, CTR, CAC |
| Klaviyo Email | retention + flows | owned | n/a | revenue/recipient, repeat rate |
| Klaviyo SMS | abandons + drops | owned | n/a | CTR, opt-out |
| Organic social (IG, TikTok) | brand + community | owned | n/a | follower growth, saves |
| SEO / blog | non-brand intent | owned | n/a | organic sessions, assisted conv |
| Influencer / UGC | proof + creative volume | paid | `<%>` | EMV, content delivered |
| Affiliate / referral | LTV expansion | paid | `<%>` | new orders from referrers |

---

## 8. Offers & Promotions

- **Default offer (always-on):** `<e.g. free ship $50+, first-order 10%>`
- **Hero promo (peak season):** `<>`
- **Subscription incentive:** `<>` (e.g. 15% off + free shipping forever)
- **Free-ship threshold:** $`<>`
- **Discount stacking rule:** `<allowed / never / specific combos>`
- **Never-discount rules:** `<e.g. never discount hero SKU below $X>`
- **Promo calendar (top 6 moments):** `<BFCM, Mother's Day, Memorial Day, Prime Day analog, founder's birthday, etc.>`

---

## 9. Compliance & Claims

- **Allowed claims (with proof):**
  - "<claim>" — proof: `<study / cert>`
- **Banned claims (regulatory or legal):**
  - "<claim>" — reason: `<FDA / FTC / EU rule>`
- **Required disclosures:** `<e.g. "results may vary", allergen disclosure, AGE21+, etc.>`
- **Reviews policy:** `<incentivized? disclosed?>`
- **Geo restrictions:** `<countries we can't ship / ad>`

---

## 10. Tech Stack

| Layer | Tool | Notes |
|---|---|---|
| Storefront | Shopify `<plan>` | theme: `<>`, page builder: `<>` |
| Email/SMS | Klaviyo | flows live: `<list>` |
| Analytics | GA4 + `<server-side?>` | UA legacy: yes/no |
| Tag manager | GTM | container ID: `<>` |
| Attribution | `<Triple Whale / NorthBeam / native>` | window: `<click/view, day count>` |
| Subscription | `<Recharge / Skio / Smartrr>` | |
| Reviews | `<Yotpo / Okendo / Judge.me>` | |
| Helpdesk | `<Gorgias / Zendesk>` | macros tracked: yes/no |
| Loyalty | `<Yotpo / Smile.io / none>` | |
| BI | `<Looker / Glew / Polar / sheets>` | |

---

## 11. KPIs & Targets

| Metric | Current | 12-month target | Trigger if breached |
|---|---|---|---|
| Blended CAC | $`<>` | $`<>` | `<action>` |
| 60-day LTV | $`<>` | $`<>` | `<action>` |
| LTV : CAC | `<:>` | 3:1 | review channel mix |
| Blended ROAS | `<>` | `<>` | `<>` |
| Site CR | `<%>` | `<%>` | LP audit |
| AOV | $`<>` | $`<>` | bundle test |
| Repeat-purchase rate (90d) | `<%>` | `<%>` | retention flow audit |
| Email rev / recipient | $`<>` | $`<>` | `<>` |
| Subscription % of orders | `<%>` | `<%>` | `<>` |

---

## 12. Brand Assets

- **Logo:** `<URL or path>` (primary, mono, reverse)
- **Color palette:** `<HEX list with names>`
- **Type system:** display `<>`, body `<>`, fallback `<>`
- **Photo & video style:** `<3 visual reference URLs + 1 paragraph description>`
- **Iconography rules:** `<>`
- **Brand book / Figma library:** `<URL>`

---

## 13. Operational Constraints

- **Margins / discount floor:** `<>`
- **Inventory headroom (next 90 days):** `<>` (don't push paid on stock-outs)
- **Shipping cost / regions:** `<>`
- **Return rate baseline:** `<%>`
- **CS SLA:** `<first reply target>`

---

## 14. Strategic Bets (next 12 months)

1. `<bet 1 — what + why + measure of success>`
2. `<bet 2>`
3. `<bet 3>`

---

## How skills consume this file

Every skill in `/skills/` declares which Brand Brain fields it needs in its frontmatter, then reads them at runtime via `{{brand.<section>.<field>}}` references. If a field is empty, the skill will ask you to fill it before proceeding — never make up brand context.
